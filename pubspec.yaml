name: tasks_app
description: "App de aprendizaje Empatia Industries."

publish_to: "none" # Do not change the publish_to value

version: 1.0.0+1

environment:
  sdk: ">=3.2.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  riverpod_annotation: ^2.3.5
  go_router: ^14.0.2
  skeletonizer: ^1.1.2+1
  animate_do: ^3.3.4
  flutter_riverpod: ^2.5.1
  path_provider: ^2.1.3
  jwt_decoder: ^2.0.1
  dio: ^5.4.3+1
  formz: ^0.7.0
  text_scroll: ^0.2.0
  change_app_package_name: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.9
  riverpod_generator: ^2.3.7
  go_router_builder: ^2.6.1

flutter:
  uses-material-design: true
  assets:
    - assets/
